import React from "react";
import PropTypes from "prop-types";

const NoCringeEditor= () => (
	<span>NoCringeEditor</span>
);

NoCringeEditor.propTypes = {
};

NoCringeEditor.defaultProps = {
};

export default NoCringeEditor;
